Three.js
    Move Path over to Three.js Path or Shape
    Move Shape over to Three.js ExtrudeGeometry
    Screen::drawShape should contain a Scene and add Shapes to it if they have no parent
    Wireframe meshes so everything is visible
    Camera
        Replace all that projection stuff
        Plug into tilt,pitch,zoom
    Other cars
        Just give each car a ready-built group
    Dashboard
        Probably needs a separate Camera
    Gradient sky
        Skybox with no texture
        Gradient material
    Everything else is a solid color
        Screen RGB and alpha are unnecessary
    White on blue wireframe support
        Doesn't have to be as pretty

Replace Print with transient text elements
    Create HTML elements that fade

Javascript prep
    Replace operators with method calls
    Replace abstracts if you haven't already

Javascript
    from Haxe, by hand

Optimization
    Build scene graph once
    Use the profiler to find bottlenecks

